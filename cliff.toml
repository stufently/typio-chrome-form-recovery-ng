# git-cliff configuration — produces CHANGELOG.md sections from
# Conventional Commits. Run locally: `git cliff -o CHANGELOG.md`.

[changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
"""
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | upper_first }}
    {% for commit in commits %}
        - {{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}\n
"""
trim = true

[git]
conventional_commits = true
filter_unconventional = false
split_commits = false
commit_parsers = [
  { message = "^feat(\\(.*\\))?!:", group = "Breaking changes" },
  { message = "^fix(\\(.*\\))?!:", group = "Breaking changes" },
  { message = "^feat(\\(.*\\))?:", group = "Added" },
  { message = "^fix(\\(.*\\))?:", group = "Fixed" },
  { message = "^refactor(\\(.*\\))?:", group = "Changed" },
  { message = "^perf(\\(.*\\))?:", group = "Changed" },
  { message = "^docs(\\(.*\\))?:", group = "Docs" },
  { message = "^test(\\(.*\\))?:", group = "Tests" },
  { message = "^build(\\(.*\\))?:", group = "Build" },
  { message = "^ci(\\(.*\\))?:", group = "Build" },
  { message = "^chore(\\(.*\\))?:", skip = true },
  { message = "^style(\\(.*\\))?:", skip = true },
]
filter_commits = false
tag_pattern = "v[0-9].*"
sort_commits = "oldest"
